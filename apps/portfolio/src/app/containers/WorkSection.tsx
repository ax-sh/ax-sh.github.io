import React from 'react';
// import { GenderToggleButton } from '@ax-sh.github.io/common';

export function WorkSection() {
  return (
    <section className={'h-screen grid place-items-center'}>
      {/*<GenderToggleButton />*/}
    </section>
  );
}
